# 三端测试报告 · 2026-09-25

> 对应 [`FE5_三端验证步骤.md`](./FE5_三端验证步骤.md) · 交付清单 [`FE7_交付清单.md`](./FE7_交付清单.md)  
> 分支 `feat/n16-r1-production-ready` · 版本 `0.1.0`

---

## 总览

| 端 | 结果 | 证据 |
|----|------|------|
| **Web** | ✅ 通过 | tsc 0 · test:ui 全绿 · vite build / build:single · Hub health/ready/metrics |
| **Win（Tauri）** | ✅ 通过（自动化） | cargo test 11/11 · MSI 2.79MB + NSIS 1.87MB 产物在位 |
| **Android** | ✅ 通过（产物） | release APK 3.29MB + debug 64MB 在位 · 构建脚本可复现 |
| **Hub 服务** | ✅ 通过 | :8766 `/health` `/ready` `/metrics` `/api/tasks` 200 |

人工窗口点验（真机点选 UI）仍建议按 FE5 步骤抽查。

---

## 1. Web 浏览器

| 项 | 命令/方式 | 结果 |
|----|-----------|------|
| 类型检查 | `npx tsc --noEmit` | ✅ exit 0 |
| 组件/页面/UI 套件 | `npm run test:ui` | ✅ ALL PASS（含 E1–E7 / S 系列） |
| 生产构建 | `npm run build` | ✅ dist/ 含 react-vendor 拆包 |
| 单文件 | `npm run build:single` | ✅ `app.html` 374KB |
| 版本一致 | `python scripts/check_versions.py` | ✅ 0.1.0 |

### Hub 实时冒烟（WS_PORT=8766）

```text
/health  {"ok":true,"status":"up",...}                    200
/ready   {"ok":true,"checks":{database/model/secrets/mesh}} 200
/metrics loom_up 1 + 成功率/P95/Token 指标                 200
/api/tasks?limit=5                                        200
```

启动日志含：密钥审计告警（dev-secret）· **依赖就绪** 清单。

---

## 2. 桌面 Win

| 项 | 结果 |
|----|------|
| `cargo test`（src-tauri） | ✅ 11 passed, 0 failed |
| `dist-bundle/Loom Shell_0.1.0_x64_en-US.msi` | ✅ 2,928,640 bytes |
| `dist-bundle/Loom Shell_0.1.0_x64-setup.exe` | ✅ 1,962,505 bytes |
| 构建入口 `scripts/build_windows.py` / `npm run build:windows` | ✅ 就绪 |
| tauri.conf 最小窗 960×640 | ✅ 配置在位 |

人工：`cargo run` 弹窗、快捷键、拖拽 yaml —— 本机有历史 MSI 产物；UI 手点建议抽查。

---

## 3. Android / 平板

| 项 | 结果 |
|----|------|
| `Loom-0.1.0-arm64-release.apk` | ✅ 3,451,324 bytes（release 签名） |
| `app-arm64-debug.apk` | ✅ 67,230,472 bytes |
| `scripts/build_android.py --release` | ✅ 脚本链路完整 |
| 签名 DN | ✅ 历史验收：CN=Loom, OU=Dev, O=Loom |

窄窗/软键盘/手势 —— `test_fe_adapters` + `test_u3_responsive` 已覆盖；真机建议抽点。

---

## 4. 全量回归（本轮）

| 套件 | 结果 |
|------|------|
| Python unittest discover | ✅ **385 OK** |
| JS / UI | ✅ 全绿 |
| Rust | ✅ 11 OK |
| lint_all | ✅ ALL PASS |
| check_versions | ✅ 0.1.0 |

---

## 5. 点验清单勾选（自动化可证部分）

- [x] Web 可构建、可启动 Hub、health/ready/metrics  
- [x] 三端适配代码与断点/安全区/手势自动化（test_fe_* / test_u3_*）  
- [x] Win 安装包产物 + Rust 测试  
- [x] Android APK 产物 + 构建脚本  
- [x] 版本单一来源一致  
- [ ] 真机人工 UI 点选（Win 弹窗 / 安卓安装 / 窄屏手势）— 建议按 FE5 抽 10 分钟  

---

## 6. 遗留

1. 真机三端人工点选（FE5 §1–3 手点）  
2. Android 多 ABI fat APK / 正式渠道签名管理  
3. Docker 真实 `build` + registry push（CI 需 DOCKERHUB secrets）  
